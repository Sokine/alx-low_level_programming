#include <stdio.h>

/**
 * main - A program that prints it's name
 * @argc: Argument count
 * @argv: Atgument vector
 *
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - argc]);

	return (0);
}
